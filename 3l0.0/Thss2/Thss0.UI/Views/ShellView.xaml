<Window x:Class="Thss0.UI.View.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Thss0.UI.View"
        mc:Ignorable="d"
        Title="ShellView" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="9*"/>
        </Grid.ColumnDefinitions>
        <StackPanel>
            <Button Command="{Binding CommandsArr[0]}">Clients</Button>
            <Button Command="{Binding CommandsArr[1]}">Professionals</Button>
            <Button Command="{Binding CommandsArr[2]}">Procedures</Button>
        </StackPanel>
        <DataGrid Grid.Column="1" ItemsSource="{Binding EntityView}" SelectedItem="{Binding SelectedEntity}"></DataGrid>
    </Grid>
</Window>
